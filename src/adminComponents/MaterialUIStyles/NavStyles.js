import { fade, makeStyles } from "@material-ui/core/styles";

export const NavUseStyles = makeStyles((theme) => ({
  navBar: {
    flexGrow: 1,
  },

  title: {
    flexGrow: 1,
  },
  appBar: {
    background: "#000",
  },
}));
